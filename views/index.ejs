<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Weather App</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
    <!-- Bootstrap -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  </head>

  <body>
        <div class = "container-fluid d-flex justify-content-center align-items-center mt-3">
            <h2 id = "title">WEATHER APP</h2>
        </div>
        
        <div class="container-fluid d-flex justify-content-center align-items-center my-5 p-5 ">
            <form action="/" method="post">
                <div class="text-center">
                    <input name="city" type="text" class="form-control form-control-lg my-2" placeholder="Enter a City" required>
                    <button type="submit" class="btn btn-lg btn-primary btn-block" >Get Weather!</button>
                </div>
            </form>
        </div>
        <div id="response" class="container-fluid d-flex justify-content-center align-items-center my-5 p-5">
            <% if(locals.weather !== null){ %>
                <p><%= locals.weather %></p>
            <% } %>
            <% if(locals.error !== null){ %>
            <p><%= locals.error %></p>
            <% } %>
        </div>

  </body>
</html>